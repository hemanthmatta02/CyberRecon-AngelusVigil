[build]
builder = "DOCKERFILE"
dockerfilePath = "infra/docker/fastapi.prod"

[deploy]
healthcheckPath = "/ready"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
